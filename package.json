{
  "name": "jojo-fitness-tracker",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Building...' && cd server && npm install && cd ../client && npm install && npm run build && echo 'Build complete'",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}